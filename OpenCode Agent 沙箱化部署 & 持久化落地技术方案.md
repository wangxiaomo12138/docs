## 一、业务背景

当前需要将离线 OpenCode Agent 能力，**完全容器沙箱化交付**，基于平台 Sidebox/AIO 临时沙箱运行用户 Agent 任务。

现有核心约束：

1. 沙箱为**临时一次性容器**，销毁后容器内所有数据丢失
2. 平台提供**用户级固定 NAS 挂载盘**：同一用户多次沙箱复用同一份持久化目录
3. 沙箱存在**最大生命周期限制（默认 900s 消杀）**，无法永久常驻
4. Skill 插件高频变更，**不能打进镜像**，需要动态用户级加载
5. 需要跨沙箱保留：会话历史、日志、执行产物、用户自定义 Skill

因此需要一套：**镜像固化程序 + NAS 承载动态数据** 的标准化沙箱部署架构。

## 二、原有问题与痛点

### 1. 原始本地运行模式问题

- 每次启动沙箱都是全新环境，**无任何会话记忆**
- 日志、会话 DB、运行产物全部在容器临时层，销毁即丢失
- Skill 全部内置包内，**无法用户动态新增、无法增量更新**
- 运行权限混乱，sudo / 普通用户交替，频繁出现权限、属主报错

### 2. 沙箱平台固有约束

- 沙箱最大存活 900s，**超长任务会被平台强制消杀**
- 不建议、也不需要常驻服务，业务为「任务型触发调用」
- 业务不能 SSH 远程调用沙箱，**只能通过平台标准 API 执行命令**

## 三、最终整体架构设计（定稿、可直接汇报）

### 整体设计思想

**程序镜像固化、数据挂载持久化、Skill 用户外置**

1. **镜像层（只读、稳定、版本化）**

- OpenCode 离线完整包
- Bun 运行时、依赖、内核能力
- 基础内置能力、基础配置

> 用途：每次拉起沙箱秒级初始化，无需重复解压、安装依赖

1. **NAS 挂载层（可写、用户隔离、永久保留）** 用户专属 `/nas/data` 目录，承载所有**动态可变数据**：

- Agent 会话历史 DB
- 运行日志
- 任务执行产物
- 用户自定义 Skill 插件

1. **调度层（平台标准化调用）**

- 后端通过 AIO 平台 API 创建沙箱
- 通过平台 Shell Exec API 执行启动脚本
- 任务结束销毁沙箱，NAS 数据永久保留

## 四、完整业务流程图（上台直接讲）

### 整体时序流程

1. **用户发起 Agent 请求**
2. **后端调用平台 API 创建用户沙箱**
   1. 自动挂载该用户专属 NAS 持久化目录
3. **沙箱启动、加载固化镜像环境**
4. **执行统一 [start.sh](start.sh) 启动脚本**
   1. 自动重定向会话、日志、产物到 NAS
   2. 自动加载 NAS 下用户 Skill
5. **OpenCode 运行任务、产生会话与日志**
6. **所有动态数据实时写入 NAS（不会随沙箱销毁丢失）**
7. **任务结束 → 后端读取产物 → 销毁沙箱**
8. **用户下次请求：新建沙箱、复用历史 NAS 数据，实现跨沙箱记忆恢复**

## 五、目录分层设计（最终标准化结构）

### 1. 镜像只读目录（不变）

内置 OpenCode 内核、bin、运行时、基础依赖

### 2. 用户 NAS 持久化目录（动态、永久）

```Plain
/nas/data/opencode/
├── sessions/      # OpenCode 会话数据库（跨沙箱记忆核心）
├── logs/          # 所有运行日志持久化
├── artifacts/     # Agent 输出产物、NDJSON结果、文件
└── skills/        # 用户动态 Skill 目录（不进镜像、可增量更新）
```

## 六、三大核心技术方案详解（本次重点分享）

### 方案一：会话历史跨沙箱持久化方案

#### 1. OpenCode 默认存储路径问题

OpenCode Linux 离线包默认会话存储：

```Plain
~/.local/share/opencode/
```

- 普通用户运行 → 存在 `/home/j/`
- sudo 运行 → 存在 `/root/` 导致**会话散乱、找不到、换用户即丢失**

#### 2. 最终解决方案

统一将 OpenCode 所有动态数据 **强制重定向到用户 NAS**

两种兜底落地方式（双保险）：

1. **优先：环境变量 / 配置指定 DataDir**
2. **兜底：软链接强制映射默认目录 → NAS 目录**

效果：

- 无论重启多少次沙箱、销毁重建
- 始终读取**同一个用户的历史会话 DB**
- 真正实现：**用户级 Agent 连续对话记忆**

### 方案二：日志 & 产物持久化方案

1. 所有运行日志不再写容器内部临时目录
2. 全部输出到：`/nas/data/opencode/logs`
3. 所有 NDJSON 执行结果、产物输出到：`/nas/data/opencode/artifacts`

解决问题：

- 沙箱消杀后无日志可排查
- 每次运行结果无法回溯

### 方案三：Skill 外置动态加载方案（核心亮点）

#### 1. 设计决策：Skill **不打入镜像**

原因：

- Skill 频繁新增、修改、迭代
- 打入镜像需要**重新打包、发版、更新镜像**，效率极低
- 多用户、多场景 Skill 差异化强，不适合全局镜像固化

#### 2. 最终方案

- 镜像只保留**内核能力**
- **用户自定义 Skill 全部存 NAS：/nas/data/opencode/skills**
- 用户第一次请求上传，**后续增量更新、无需重复上传**

#### 3. 实现方式

修改 OpenCode 加载逻辑（二选一）

1. 支持多路径：**镜像基础 Skill + NAS 用户 Skill 双加载**（最优）
2. 不支持多路径则软链接兜底

优势：

- 镜像长期稳定不动
- 用户技能完全隔离、独立、可迭代
- 支持千人千面的 Agent 能力

## 七、沙箱 900 秒消杀适配方案（关键架构约束）

### 1. 平台硬性限制

沙箱最长生命周期 900 秒，**无法永久常驻** 未来可申请拉长，但**永远存在最大时长限制**

### 2. 影响

- 超长任务会被平台强制杀进程、杀容器
- 传统 serve 常驻服务模式**不适合本平台**

### 3. 最终适配架构

**放弃常驻 Serve 模式，采用任务式 Run 模式**

- 业务不依赖端口暴露、不依赖 HTTP 常驻服务
- 每次任务**临时拉起沙箱 → 执行 → 销毁**
- 所有中间状态、日志、会话全部落 NAS，不怕消杀

> 这是适配当前 AIO 沙箱平台的**最优工程模式**

## 八、业务最终调用模式（确定版）

**业务不使用 SSH、不使用端口代理、不依赖 Serve 服务**

标准业务链路： 后端 → AIO 平台 API（创建沙箱） → 平台 ShellExec 执行 [start.sh](start.sh) → OpenCode Run 执行任务 → 产物落 NAS → 后端读取 NAS 结果 → 销毁沙箱

一期完全不依赖端口代理，**不需要问平台端口开放问题**，可直接上线。

## 九、现存风险点（汇报必须讲）

1. **单用户并发风险** 同一用户同时多沙箱并发，会并发读写 sqlite 会话 DB 可能出现数据库锁、损坏 → 解决方案：业务层**用户级别串行限流**
2. **900s 超时风险** 超长任务会被消杀 → 后续可申请平台延长沙箱时长
3. **Skill 热更新限制** NAS 新增 Skill 需要**新沙箱重启加载** 但本业务是任务式触发，天然适配无影响

## 十、当前剩余待攻克技术点（清晰落地清单）

1. 定位 OpenCode 官方 DataDir，完成会话目录 NAS 重定向
2. 完成日志、产物路径全部迁移 NAS
3. 配置 Skill 外部 NAS 加载路径（核心）
4. 镜像权限固化，彻底消除 sudo 权限坑
5. 验证跨沙箱会话恢复、Skill 增量加载
6. 确认平台 ShellExec 长任务超时阈值

## 十一、方案优势总结（汇报收尾话术）

1. **完全贴合平台沙箱规范**，不突破平台限制、不依赖特殊能力
2. **镜像与数据完全解耦**，稳定性极高
3. **用户数据永久留存**，实现 Agent 跨会话、跨沙箱记忆
4. **Skill 动态可迭代**，无需频繁更新镜像
5. 适配平台 900 秒消杀机制，任务式架构稳定可靠
6. 权限、日志、会话、插件全部标准化，可直接量产落地