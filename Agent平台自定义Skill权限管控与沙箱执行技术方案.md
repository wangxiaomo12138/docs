# Agent平台自定义Skill权限管控与沙箱执行技术方案

# 一、方案概述

## 1\.1 项目背景

现有Agent平台支持用户上传自定义Skill、基于Skill发起自动化任务执行，但当前平台缺乏精细化的Skill权限隔离能力，所有用户默认可见全部Skill资源，存在核心自定义技能泄露、非法调用、数据越权访问等安全风险。同时，Skill任务执行无独立隔离环境，恶意Skill脚本、异常任务可能影响平台主服务稳定性、污染业务数据。

为解决以上问题，本方案构建**基于角色与用户的精细化Skill权限体系**，实现Skill资源分级可见、可控使用，并配套独立沙箱执行环境，保障自定义Skill的安全上传、权限隔离、隔离执行，兼顾平台灵活性、扩展性与安全性。

## 1\.2 核心目标

- **权限精细化管控**：实现Skill资源按用户、角色、团队分级可见、可用，支持私有、团队、公开三种权限层级，杜绝越权查看和使用Skill。

- **任务安全隔离执行**：所有自定义Skill任务均在独立沙箱环境运行，实现资源隔离、数据隔离、权限收敛，避免恶意脚本、异常任务破坏主平台服务。

- **流程闭环自动化**：完成Skill上传、权限配置、可见性控制、任务拉起、沙箱执行、结果返回、日志审计的全流程自动化。

- **高可扩展性**：权限模型、沙箱环境支持横向扩展，适配后续用户量、Skill数量、任务并发量增长，支持Skill版本管理、细粒度调用权限扩展。

## 1\.3 适用范围

本方案适用于Agent平台所有自定义Skill的上传、权限配置、展示浏览、任务发起、沙箱执行、运维审计全生命周期管理，覆盖普通用户、管理员、Skill创作者、团队管理员等全角色用户。

# 二、总体架构设计

## 2\.1 架构整体思路

采用**权限分层管控\+微服务解耦\+沙箱独立隔离**的架构设计，将平台拆分为用户权限层、Skill资源管理层、任务调度层、沙箱执行层、数据存储层五大核心层级，各层级独立部署、职责解耦，通过统一接口联动，保障系统稳定性和安全性。

## 2\.2 核心架构分层

### 2\.2\.1 用户权限层

基于RBAC权限模型扩展，构建用户\-角色\-团队\-Skill资源的四维权限关联体系，负责用户身份认证、Skill可见性校验、任务调用权限校验，是整个系统的权限入口。

### 2\.2\.2 Skill资源管理层

承载用户自定义Skill的上传、解析、存储、版本管理、元数据维护，记录Skill归属、权限属性、执行配置，为权限管控和任务执行提供资源支撑。

### 2\.2\.3 任务调度层

接收用户Skill任务发起请求，前置权限校验，合法任务统一调度分发，负责沙箱环境拉起、任务状态同步、执行进度管控、结果回调。

### 2\.2\.4 沙箱执行层

独立于主平台的隔离执行环境，为每一个Skill任务分配独立沙箱实例，限制资源占用、网络访问、文件操作、系统调用，实现任务隔离执行与安全管控。

### 2\.2\.5 数据存储层

分别存储用户权限数据、Skill元数据、任务日志、沙箱执行记录、审计日志，区分结构化数据与文件资源，保障数据可追溯、可统计。

## 2\.3 整体业务流程

用户登录平台→权限层身份校验→前端根据用户权限筛选展示可查看Skill→用户选择Skill并发起任务→任务调度层校验调用权限→按需拉起独立沙箱环境→沙箱内执行Skill任务→执行结果回写平台→销毁/回收沙箱资源→全程日志审计留存。

# 三、核心权限体系设计

## 3\.1 权限模型设计

基于经典RBAC模型扩展，新增**Skill资源权限维度**，突破全局角色权限限制，实现单Skill级别的精细化权限控制，核心关联关系：用户\-角色\-团队\-Skill权限策略。

### 3\.1\.1 角色定义

- **超级管理员**：拥有所有Skill的查看、编辑、删除、调用权限，可配置所有用户/团队的Skill权限。

- **团队管理员**：管理所属团队内Skill权限，可设置团队Skill的公开范围、成员使用权限。

- **Skill创作者**：拥有自己上传Skill的全部权限，可自主设置Skill可见性、授权其他用户/团队。

- **普通用户**：仅可查看、调用权限开放的Skill，无编辑、授权权限。

### 3\.1\.2 Skill权限等级（核心）

所有自定义Skill默认分为三种可见权限模式，创作者可随时切换，管理员可兜底管控：

- **私有权限（默认）**：仅Skill上传创作者、超级管理员可见、可调用，其他用户无法检索、查看、使用该Skill，保障个人自定义技能隐私。

- **团队权限**：创作者授权指定团队/部门可见，团队内所有用户可检索、调用，团队外用户无任何访问权限，适用于团队共享技能。

- **公开权限**：平台所有已登录用户均可查看、调用，适用于通用公共Skill，管理员可审核公开Skill，禁止违规技能公开。

## 3\.2 权限校验逻辑

### 3\.2\.1 Skill列表展示校验

用户进入Skill市场/我的Skill页面时，后端根据当前用户身份、所属团队，批量过滤Skill资源：仅展示私有（本人创建）、团队（所属团队授权）、公开（平台通用）的Skill，屏蔽无权限资源，前端无任何无权限数据残留。

### 3\.2\.2 任务发起权限校验

用户发起Skill任务时，后端二次校验权限，避免前端权限绕过：若用户无该Skill调用权限，直接拦截请求，返回权限不足提示，同时记录越权访问日志。

### 3\.2\.3 权限优先级

私有权限 \> 团队权限 \> 公开权限，专属权限优先于通用权限，支持单个Skill对指定用户单独授权（特殊场景兜底）。

# 四、自定义Skill全流程管理

## 4\.1 Skill上传与接入

平台支持用户通过前端界面上传自定义Skill包（脚本、配置文件、依赖包等），后端完成自动解析、格式校验、合规检测，具体流程如下：

1. 用户上传Skill资源包，填写Skill名称、描述、功能标签、执行参数、依赖环境等元数据；

2. 平台自动校验文件格式、脚本合法性、是否包含高危指令，拦截恶意违规Skill；

3. 上传成功后默认标记为**私有权限**，仅创作者可见；

4. 支持创作者编辑权限模式，切换为团队/公开权限，支持版本迭代更新，留存版本记录。

## 4\.2 Skill资源分类展示

前端根据用户权限差异化展示Skill列表，分为三大模块：我的私有Skill、团队共享Skill、平台公开Skill，用户可按需检索、筛选、收藏，无权限资源完全隐藏，提升使用体验。

## 4\.3 Skill权限编辑与授权

- 创作者可对自有Skill进行权限配置，支持指定具体团队、单个用户授权访问；

- 团队管理员可统一管理团队内所有共享Skill，可回收权限、禁止违规Skill共享；

- 超级管理员拥有全局权限，可强制修改任意Skill的权限等级、下架违规Skill。

# 五、沙箱环境执行体系设计

## 5\.1 沙箱核心设计原则

为彻底隔离自定义Skill执行风险，所有Skill任务**必须在独立沙箱中执行**，禁止直接在主平台容器/服务器运行，遵循四大原则：资源隔离、环境纯净、权限最小化、用完即销毁。同时针对**接口调用类Skill、自定义私有服务部署**场景，支持用户自主制作、上传专属沙箱镜像，通过自定义端口映射暴露内部服务，支撑Agent模型调用私有接口服务，适配复杂业务接口流程执行需求。

## 5\.2 沙箱环境架构

采用**轻量化容器沙箱\+自定义镜像适配**方案，基于Docker构建独立隔离环境，支持平台通用镜像、用户自定义镜像双模式拉起，具备镜像上传存储、动态部署、端口映射、弹性扩容、自动回收能力，完美适配普通脚本Skill、接口服务类Skill的多场景并发执行需求。

### 5\.2\.1 沙箱环境隔离能力

- **资源隔离**：单沙箱独立CPU、内存、磁盘资源配额，限制最大资源占用，避免单任务耗尽平台资源；

- **系统隔离**：沙箱为独立容器，与主平台系统、其他任务沙箱完全隔离，无法访问宿主系统文件、进程；

- **网络隔离**：默认严格隔离外网访问，仅开放平台内部任务回调内网链路，防止Skill恶意外联、数据泄露；支持按Skill粒度显性配置外网白名单、放行指定端口/域名，实现可控外网接口调用。

- **权限隔离**：沙箱内执行用户为最低权限账号，无系统修改、文件写入、进程创建等高权限操作能力。

### 5\.2\.2 沙箱环境配置

- 基础环境：预置Python、Shell等通用运行环境，同时支持用户自定义镜像环境导入，适配各类接口服务运行依赖；

- 资源限制：单任务默认内存2G、CPU1核，支持根据Skill配置动态调整，超时任务自动强制终止，防止资源挤占；

- 数据隔离：沙箱内仅临时存储任务执行数据，任务结束后自动清空临时文件，无数据残留；

- **自定义镜像能力**：支持用户基于基础镜像制作专属业务镜像，内置自研服务、接口依赖、请求配置，可自主上传至平台镜像仓库，绑定对应Skill；

- **端口映射能力**：支持用户自定义配置容器端口与宿主机内网端口映射，仅开放内网可访问，对外屏蔽真实服务端口，安全暴露接口服务供模型调用。

## 5\.3 任务沙箱执行全流程

1. **任务接收与校验**：调度层接收用户任务请求，完成Skill权限校验、参数合法性校验，识别当前Skill是否为「自定义镜像\+接口服务」类型；

2. **沙箱动态拉起**：普通脚本Skill使用平台通用镜像拉起，接口服务类Skill加载用户上传的专属镜像，初始化容器环境、加载Skill接口流程配置；

3. **端口映射配置**：沙箱启动阶段自动加载用户预配置端口映射规则，完成容器服务端口与内网宿主机端口绑定，仅开放模型调用白名单权限，禁止外网访问；

4. **隔离执行任务**：沙箱内启动用户自定义服务，运行接口调用类Skill逻辑，Agent模型通过内网映射端口调用沙箱内私有服务，全程监控任务进度、资源占用、接口请求异常；

5. **结果回写**：任务执行完成后，将接口调用结果、服务日志、报错信息回传至平台数据库，展示给用户；

6. **沙箱销毁回收**：无论任务成功、失败、超时，执行结束后立即销毁沙箱容器、释放映射端口，彻底回收系统资源，杜绝端口占用、服务残留风险；

7. **异常兜底**：任务超时、服务启动失败、接口请求异常、资源溢出时，自动终止任务，释放端口资源，记录异常日志并推送告警信息。

## 5\.4 并发调度策略

平台支持多用户、多Skill任务、多自定义服务并发执行，采用队列调度机制：任务进入等待队列，按优先级排序，系统根据空闲资源动态拉起对应数量沙箱、分配空闲内网端口，避免端口冲突、并发过载，保障平台及用户私有服务稳定运行。针对同用户多服务场景，支持端口自动递增分配、端口复用隔离策略，杜绝服务端口抢占冲突。同时针对Nitrobox沙箱提供标准化网络配置能力，支持按Skill独立开关外网隔离、配置端口白名单，适配需要调用外部公网接口的业务场景。

## 5\.6 主流沙箱选型对比（agent\-infra/sandbox vs opensage\-agent/nitrobox）

结合本方案**用户自定义镜像上传、常驻接口服务、内网端口映射、模型内网调用、用完即销毁**核心诉求，对当前备选两款沙箱进行技术原理、能力适配、优缺点、生产落地性全方位对比，为平台最终选型提供依据。

### 5\.6\.1 两款沙箱核心技术原理

#### （1）agent\-infra/sandbox（AIO Sandbox）

基于标准Docker容器封装的一体化Agent沙箱，依托Linux Namespace\+Cgroups实现基础资源与系统隔离，主打**全场景集成化环境**，内置终端、文件服务、网络代理、浏览器、VSCode Server等配套能力，网络视图与宿主机打通，无需复杂网络配置即可实现内网互通。整体定位为开发、演示、PoC场景的一体化运行沙箱，适配通用Agent任务执行场景。

#### （2）opensage\-agent/nitrobox

轻量高性能用户态沙箱，基于**User Namespace\+OverlayFS\+seccomp\-bpf\+Landlock**多层内核加固隔离，无需Root权限运行。主打极致启停性能与精细化安全策略，**支持运行时文件创建/读写、Shell命令执行、动态代码运行**，支持毫秒级实例创建/销毁、进程状态快照恢复、原生TCP端口转发，专为AI Agent高频短任务、代码执行、轻量化服务常驻场景设计。网络层面默认开启严格隔离策略，仅保留本地回环网络、默认禁止外网主动出站访问，可通过显性配置关闭网络隔离、放行指定端口与域名白名单，实现可控外网访问；文件系统采用**系统目录只读、用户工作目录完全可读写**的分层隔离策略，禁止运行时修改系统全局Python/系统依赖，但支持用户在自定义工作目录创建虚拟环境并动态安装三方依赖。

### 5\.6\.2 核心能力与本方案适配度对比

| 对比维度             | agent\-infra/sandbox                                         | opensage\-agent/nitrobox                                     | 方案适配结论                                                 |
| -------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **自定义镜像支持**   | 完全兼容标准Docker镜像，支持用户自制业务镜像导入、绑定Skill，同时**支持沙箱运行时动态安装Python/系统依赖、文件读写、Shell执行**，适配调试型、动态依赖、自定义服务运行场景 | 兼容OCI标准镜像，支持自定义镜像预打包环境，**支持运行时文件读写、创建文件、执行Shell/Python脚本**，具备完整Agent代码执行能力；网络访问采用**默认隔离、按需配置放行**机制：默认全网隔离、禁止主动外网出站请求，支持通过平台配置显性放开外网权限、配置端口/域名白名单。文件系统分层隔离：系统目录只读，无法运行 apt 系统安装、无法修改全局 Python 环境；**用户工作目录可完全读写，在外网放行前提下，支持创建 python venv 虚拟环境、动态 pip 安装三方包**。 | 两款均支持本方案自定义镜像核心能力                           |
| **端口映射能力**     | 依托Docker原生端口映射，配置简单、兼容性强，支持固定/动态端口映射，内网互通稳定 | 内置pasta NAT端口转发组件，原生支持TCP端口映射，无依赖、毫秒级端口绑定，冲突规避能力更强 | nitrobox端口转发更轻量化、高性能；agent\-infra配置更通用易用 |
| **常驻接口服务适配** | 容器生命周期管理成熟，支持服务常驻运行、日志持久化，适配长时间接口服务调用场景 | 默认极致短任务优化，支持进程常驻，适配高频轻量接口调用，启停切换效率极高 | 长时稳定服务优先agent\-infra；高频瞬时服务优先nitrobox       |
| **启停性能**         | 容器拉起数百毫秒级，资源初始化完整，启动稍慢但环境稳定性高   | 极致轻量化，创建约7ms、销毁约2ms，瞬时扩容能力极强，适配高并发任务 | 高并发场景nitrobox优势显著                                   |
| **安全隔离等级**     | 容器级隔离，共享宿主机内核，适合内网可信场景，公网暴露存在一定风险 | 多层内核安全加固，默认禁用高危系统调用、只读文件路径、权限收敛，隔离粒度更细、安全性更高 | 生产安全场景nitrobox更优                                     |
| **权限与网络管控**   | 网络与宿主机打通，审计便捷，支持白名单限流，配置成本低       | 支持声明式安全策略、流量精准管控、无Root运行，默认网络严格隔离，支持显性配置外网白名单与放行端口，越权防护、流量管控能力更强 | 精细化管控需求优先nitrobox                                   |
| **运维复杂度**       | 生态成熟、Docker体系通用，运维成本低、问题排查简单           | 轻量自研机制，脱离传统Docker体系，运维心智更高，社区生态偏小 | 快速落地、低运维成本优先agent\-infra                         |

### 5\.6\.3 两款沙箱优缺点详细拆解

#### （1）agent\-infra/sandbox 优缺点

**核心优点**：一是生态成熟，完全兼容Docker镜像与容器规范，用户自定义镜像上传、打包、映射流程无需改造，适配本方案现有镜像体系；二是集成度高，内置终端、文件、网络、日志全套能力，接口类Skill的服务运行、日志采集、异常排查更便捷；三是网络适配友好，沙箱网络视图与宿主机一致，内网模型调用无需复杂转发配置，稳定性强；四是长任务稳定性优异，适配用户自定义服务长时间常驻、反复接口调用场景。

**核心缺点**：一是启动性能偏弱，容器拉起耗时较高，高并发任务场景扩容速度受限；二是隔离等级为标准容器级，共享宿主机内核，相较于内核级隔离存在一定安全短板；三是资源开销相对较高，单实例资源占用高于轻量化沙箱，大规模并发部署成本更高。

#### （2）opensage\-agent/nitrobox 优缺点

**核心优点**：一是极致性能，毫秒级启停、瞬时销毁，完美适配平台高频并发Skill任务，端口资源释放极快，彻底杜绝端口积压冲突；二是安全性更强，多层内核加固、默认最小权限、无Root运行，有效防范恶意镜像、异常脚本越权与逃逸；三是端口转发原生优化，内置NAT转发能力，零依赖实现精准端口映射，适配多服务、多端口并发隔离；四是支持进程快照恢复，适配Agent重试、迭代执行场景，任务容错性更高。

**核心缺点**：一是生态相对小众，脱离传统Docker运维体系，运维人员学习成本高，线上问题排查资料较少；二是采用分层文件系统隔离策略，**禁止修改系统全局环境、禁止运行系统级装包命令**，但支持用户目录虚拟环境动态装包，存在环境使用门槛；所有系统级固定依赖建议提前预制到自定义镜像中；三是长时常驻服务适配略弱于标准容器，长时间运行的自定义接口服务稳定性需额外验证；四是部分高级容器特性适配有限，复杂依赖的自定义镜像可能存在兼容适配问题。

### 5\.6\.4 适配本方案的最终选型建议

结合本方案**「自定义镜像\+常驻接口服务\+内网端口映射\+可控外网调用\+运行时虚拟环境装包\+模型调用\+权限隔离\+生产稳定」**的综合诉求，针对两款沙箱的环境自定义能力差异，精准分场景给出选型方案：

- **优先选型：agent\-infra/sandbox（通用生产/调试场景）**：适配大部分用户自定义镜像、长时间接口服务运行、稳定端口映射调用场景，**支持运行时动态安装Python/系统依赖**，兼容调试类、动态依赖类Skill，依托成熟Docker生态保障落地速度与运维稳定性，完美兼容现有Skill上传、镜像绑定、权限管控流程，适合初期落地、业务迭代与规模化上线。

- **优先选型：opensage\-agent/nitrobox（高安全、标准化生产场景）**：适合平台高安全等级需求、高频瞬时Skill任务、多用户高密度并发、敏感接口服务调用场景。采用分层安全策略，系统环境只读防篡改，支持外网按需白名单放行，**支持用户目录自建 venv 虚拟环境、动态 pip 安装业务依赖**，适合需要可控外网调用、运行时动态安装业务包、且禁止系统环境篡改的标准化生产Skill，凭借更强的隔离能力、极致启停性能、精细化权限策略，提升平台安全上限与并发承载能力，适合后期性能优化与安全升级迭代。

- **混合适配方案（推荐长期架构）**：平台可做沙箱层抽象适配，同时兼容两款沙箱，普通常驻接口Skill默认使用agent\-infra/sandbox，高并发、高权限敏感Skill自动调度至nitrobox，兼顾落地效率、稳定性与安全性能。

## 5\.5 用户自定义镜像与端口映射专项设计

针对接口调用类Skill需要依托自研服务、私有依赖、定制运行环境执行的场景，平台开放**用户自定义沙箱镜像上传\+专属端口映射\+内网服务暴露**完整能力，支持用户自主制作业务镜像、绑定对应Skill、配置端口转发，实现Agent模型内网安全调用自定义服务，全程环境隔离、权限隔离、资源隔离，不侵入主平台环境。

### 5\.5\.1 自定义镜像规范与制作标准

平台提供统一基础镜像模板（Linux\+通用运行依赖），用户可基于基础镜像二次定制，适配自身接口服务运行需求，镜像制作需遵循平台规范，保障兼容性与安全性：

- **基础约束**：必须基于平台提供的标准基础镜像构建，禁止从零构建陌生系统镜像，规避系统兼容与底层漏洞风险；镜像内仅保留业务服务、接口依赖、运行脚本，禁止预装高危工具、代理程序、穿透工具。

- **服务规范**：用户自定义服务需支持前台常驻运行、可配置启动命令、无本地固定端口硬编码，适配平台动态端口映射机制；服务启动需支持超时退出、异常自终止，避免沙箱残留进程。

- **安全规范**：镜像内禁止存储明文密钥、账号密码、业务敏感凭证，所有接口凭证、配置参数统一通过平台任务参数传入、临时注入，不落地存储。

- **体积约束**：镜像压缩包大小限制在合理范围，精简冗余文件，提升沙箱拉起速度，降低存储与传输开销。

### 5\.5\.2 镜像上传、审核与绑定流程

平台搭建私有镜像仓库，隔离存储用户自定义镜像，与平台官方镜像物理隔离，全程权限管控、安全校验，具体流程如下：

1. **镜像打包**：用户按照平台规范完成自定义镜像制作、打包，生成标准镜像压缩文件，填写镜像名称、适配Skill、运行环境、启动命令、端口需求等元数据。

2. **上传检测**：用户上传镜像至平台，系统自动执行安全检测，包含漏洞扫描、高危程序检测、违规端口校验、敏感信息检测，拦截不合格镜像。

3. **权限入库**：检测通过后镜像存入用户私有镜像仓库，默认仅创作者可见可用，遵循Skill三级权限体系，支持跟随Skill授权团队共享。

4. **Skill绑定**：用户可将自定义镜像与对应接口类Skill一对一/一对多绑定，后续该Skill发起任务时，自动优先加载绑定的专属镜像拉起沙箱环境。

### 5\.5\.3 精细化端口映射机制

为实现沙箱内自定义服务安全暴露、供Agent模型调用，平台设计**内网动态端口映射方案**，全程不暴露公网端口，仅平台内网模型可访问，杜绝外网泄露与恶意访问风险。

- **端口配置模式**：支持两种配置方式，一是用户预配置固定端口映射（容器内部服务端口→内网宿主机指定端口），适配固定端口服务；二是系统动态分配空闲端口，自动规避端口冲突，适配多服务并发场景。

- **网络隔离策略**：映射端口仅开放平台内网白名单IP，仅Agent模型服务、平台调度服务可访问，彻底关闭外网访问权限；单沙箱端口独立隔离，不同用户、不同任务的服务端口互不连通。

- **端口生命周期管控**：端口映射与沙箱实例生命周期强绑定，任务拉起、沙箱启动时自动生效，任务结束、沙箱销毁后立即释放端口，无永久端口占用，杜绝端口资源耗尽、残留监听风险。

- **端口访问限流**：支持对映射端口配置访问频次、并发数限制，防止自定义服务被高频调用挤占平台资源，保障服务稳定性。

### 5\.5\.4 模型调用自定义服务流程

接口类Skill依托自定义镜像\+端口映射，实现模型调用私有业务服务的完整闭环流程：

1. 用户发起绑定自定义镜像的接口类Skill任务，调度层完成权限校验后，拉起专属镜像沙箱；

2. 沙箱启动后自动执行用户预设启动命令，拉起内部自定义服务，完成端口映射绑定，生成内网可访问服务地址；

3. Skill脚本读取平台分配的内网端口与服务地址，生成模型可调用的接口请求链路；

4. Agent模型通过内网白名单地址\+映射端口，安全调用沙箱内私有服务，完成接口请求、数据交互、业务流程执行；

5. 任务执行完毕后，沙箱销毁、端口释放，所有临时服务资源彻底回收，无数据与服务残留。

### 5\.5\.5 镜像与端口权限管控规则

- 用户自定义镜像归属对应创作者，权限跟随绑定Skill，Skill为私有则镜像与端口仅本人可用，Skill团队共享则团队用户可复用镜像与端口服务能力；

- 未授权用户无法加载、查看、使用他人自定义镜像，无法访问非自有沙箱映射端口，杜绝跨用户服务越权访问；

- 管理员可审计所有自定义镜像、端口映射、接口调用记录，可下架违规镜像、封禁异常端口访问行为。

# 六、核心业务全流程闭环

## 6\.1 Skill上传授权流程

用户上传Skill → 格式/合规校验 → 默认私有权限入库 → 创作者配置权限（私有/团队/公开） → 权限策略生效 → 对应用户可见。

## 6\.2 Skill任务执行流程

用户选择可见Skill → 填写任务参数 → 发起执行请求 → 后端权限二次校验 → 调度层接收任务 → 拉起独立沙箱 → 隔离执行Skill → 结果日志回存 → 销毁沙箱 → 用户查看结果/日志。

## 6\.3 权限变更生效流程

管理员/创作者修改Skill权限 → 更新权限策略数据库 → 实时刷新用户前端Skill展示列表 → 后续任务执行按新权限校验，即时生效无延迟。

# 七、安全与审计设计

## 7\.1 多层安全防护

- **上传安全**：校验Skill文件格式、查杀恶意脚本、拦截高危系统指令，禁止违规Skill上传；自定义镜像上传自动进行病毒扫描、漏洞检测、高危端口拦截，禁止搭载恶意服务、违规代理的镜像入库。

- **权限安全**：前后端双重权限校验，杜绝前端绕过、越权调用，细化资源访问粒度；用户自定义镜像、映射端口仅本人及授权团队可见，禁止未授权用户访问私有服务。

- **执行安全**：沙箱全隔离执行，资源、网络、权限三重限制，杜绝恶意脚本破坏系统、泄露数据；端口映射仅开放内网白名单，限制调用IP、调用频次，防止接口被恶意请求。

- **数据安全**：任务执行数据脱敏存储，Skill源码、自定义镜像、接口密钥配置加密存储，仅授权人员可查看、使用，杜绝接口凭证、业务数据泄露。

## 7\.2 全流程日志审计

平台全程记录核心操作日志，支持溯源、排查、合规审计，核心日志类型：

- Skill操作日志：上传、编辑、权限修改、删除记录；

- 权限访问日志：越权访问、Skill查看、授权记录；

- 任务执行日志：任务发起、执行进度、报错信息、沙箱运行记录；

- 运维操作日志：管理员权限配置、Skill下架、系统管控操作。

# 八、扩展性与兼容设计

## 8\.1 权限扩展

现有权限模型支持横向扩展，后续可新增**Skill调用频次限制、参数白名单、定时执行权限****、自定义镜像授权、端口调用权限管控**等细粒度管控能力，适配企业级精细化权限需求。

## 8\.2 沙箱扩展

沙箱全面支持用户自定义镜像模板，可适配各类自研接口服务、特殊依赖环境的Skill执行，支持单Skill绑定专属镜像、独立端口映射配置；同时支持GPU资源沙箱、高并发专属沙箱集群扩展，适配复杂接口服务、高频率模型调用场景。

## 8\.3 功能兼容

完全兼容现有平台Skill上传、任务执行核心能力，无需改造原有业务逻辑，权限管控与沙箱执行以插件化、服务化方式接入，无缝适配存量业务。

# 九、部署与落地规划

## 9\.1 落地阶段

- **第一阶段（基础能力）**：完成权限体系搭建，实现Skill三级权限管控、差异化展示、前后端权限校验；

- **第二阶段（沙箱能力）**：搭建轻量化容器沙箱集群，实现Skill任务隔离执行、资源限制、自动销毁；

- **第三阶段（优化迭代）**：完善日志审计、异常告警、并发调度优化，扩展细粒度权限与沙箱定制能力。

## 9\.2 运维保障

搭建沙箱资源监控、任务监控、权限操作监控体系，实时监控沙箱资源占用、任务失败率、越权访问行为，异常情况及时告警，保障平台稳定安全运行。

# 十、方案总结

本方案基于现有Agent平台能力，针对性解决自定义Skill**权限无隔离、执行无防护****、私有接口服务无法部署调用**三大核心问题，通过三级Skill权限体系实现资源精细化管控，基于容器沙箱实现任务安全隔离执行，新增**用户自定义镜像上传、专属环境部署、内网端口映射、模型私有服务调用**能力，完美适配普通脚本Skill与接口流程类Skill双场景。方案架构清晰、兼容性强、扩展性高，既保障了普通用户自定义Skill的隐私安全，又兼顾了团队共享与通用能力开放，同时彻底规避了恶意Skill、异常任务对主平台的安全风险，可完全支撑平台商业化、企业级落地使用。

> （注：部分内容可能由 AI 生成）
