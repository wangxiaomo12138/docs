# Agent沙箱方案客户答疑文档
## 一、问题1：目前很多Agent框架已经实现本地沙箱隔离，和本方案有什么区别，为什么要采用当前这套方案？
### 1.1 框架自带本地沙箱是什么
市面上多数Agent框架内置的本地沙箱，属于**进程级/语言级软沙箱**。实现逻辑是在Agent主进程内拉起受限子进程，仅对**单段工具代码执行**做限制，管控文件读写、系统调用。
核心特点：**只隔离代码片段，Agent主进程本身依旧运行在宿主机上**。

### 1.2 框架内置本地沙箱的局限性
1. **隔离范围有限**：仅隔离工具执行片段。Agent主进程、Skill加载逻辑、会话读写、配置信息都在宿主机。即使执行代码被限制，恶意Skill仍有机会读取宿主机环境变量、跨用户目录、其他用户配置。
2. **隔离强度弱，存在逃逸风险**：依靠Linux namespace、seccomp做软件层面限制，属于软隔离，历史上同类语言沙箱多次出现逃逸漏洞。
3. **无法实现整套环境销毁重建**：Agent主进程常驻，任务结束后环境不会整体销毁，宿主机残留缓存、临时文件与状态，无法适配平台超时自动消杀、环境重置机制。
4. **缺少平台持久化能力**：仅进程内能力，不能对接平台用户独立NAS存储，无法实现用户Skill、会话数据持久挂载。
5. **资源缺少统一管控**：资源配额、回收、并发调度全部需要业务自行开发，不适合大规模多租户场景。

### 1.3 我方AIO Sidebox平台容器沙箱
采用完整容器级硬隔离，**将整套Agent运行环境隔离**。独立容器拥有独立内核视图、独立文件系统。
> 本方案采用双层安全防护：外层AIO平台容器沙箱，隔离整套Agent运行环境；内层复用Agent框架自带沙箱，对工具代码做二次细粒度隔离。

### 对比汇总表
|对比项|Agent框架自带本地沙箱（进程/语言级软沙箱）|我方AIO平台Sidebox容器沙箱|
| ---- | ---- | ---- |
|隔离层级|进程内，仅隔离工具代码片段|完整容器，隔离整套Agent运行环境|
|隔离强度|软件软隔离，存在逃逸风险|容器硬隔离，安全等级更高|
|整体环境销毁|Agent主进程常驻，无法整体销毁|容器可一键销毁，环境完全重置|
|NAS持久化挂载|无平台挂载能力|支持按用户挂载独立NAS盘|
|资源调度|业务自行管控资源|平台统一配额、超时消杀、资源回收|
|适用场景|单租户、可信内网，仅需隔离代码执行|多租户对外业务、支持用户自定义上传Skill，强安全+资源管控|

### 客户一句话应答
框架自带沙箱仅仅隔离Agent内部执行的小段工具代码，Agent主体程序仍然跑在宿主机；我们的平台容器沙箱是把Agent整套运行环境完整隔离，同时由平台统一负责环境销毁、NAS持久化、资源调度。框架自带沙箱无法满足我们多租户、用户自定义Skill、平台自动消杀的核心需求，因此需要叠加平台AIO容器沙箱。

## 二、问题2：Agent调用沙箱（Agent在外，沙箱仅跑工具） 和 Agent运行在沙箱内，二者差异原理详解
> 核心结论：两种架构本质差异不在于代码存放位置，而在于**Agent完整运行上下文（推理循环、Skill加载器、会话读写模块）部署在哪里**。
> Agent不是一次性脚本，是持续的ReAct推理循环：接收消息→思考决策→加载Skill→调用工具→读写会话历史→继续多轮思考。这套推理循环就是Agent核心运行上下文。

### 方案A：Agent在外，仅工具代码下发至沙箱执行
**架构**：外部常驻服务承载Agent推理主循环（思考、会话读写、Skill决策）；沙箱作为临时容器，只接收Agent下发的工具代码片段，执行完成后返回结果。
沙箱内部**不存在Agent推理主循环**，只充当被动的远程代码执行器。

**核心短板**
Agent的Skill加载、会话数据库读写、推理决策全部保留在外部常驻服务。
如果要实现每个用户独立私有Skill，外部常驻Agent进程就需要动态切换、加载不同用户Skill。多用户并发场景下会出现：
1. 不同用户Skill加载在同一个常驻进程，内存缓存互相串扰、污染；
2. 频繁热加载Skill，性能下降、稳定性差；
3. 用户Skill依赖包、配置文件无法做到环境隔离；
4. NAS挂载与会话读写逻辑在外部，沙箱感知不到用户独立环境。

> 本质：沙箱只负责执行工具片段，Agent完整生命周期、上下文不在沙箱内。

### 方案B：Agent完整运行在沙箱内部（我方采用方案）
**架构**：沙箱容器内部完整启动Agent推理ReAct循环、Skill加载器、会话读写模块。外部后端仅负责拉起沙箱实例，等待沙箱内Agent执行完成后获取最终结果。

**核心特点**
Agent全部运行上下文（推理循环、Skill加载、会话读写）都在独立沙箱容器内部。每拉起一个新沙箱，就代表一套全新独立Agent实例。
1. 沙箱内Skill加载器，仅读取当前用户NAS目录下专属Skill；
2. 沙箱内会话读写，仅操作当前用户NAS中的会话数据；
3. 沙箱销毁，整套Agent实例随容器一起销毁，不留残留状态。

**优势根源**
独立容器天然隔离Agent实例，原生适配多租户。不需要外部常驻进程做复杂的Skill动态切换，沙箱启动时直接加载对应用户NAS下的Skill，不存在多用户缓存串扰、环境互相污染问题。

### 两种模式总结
Agent在外调用沙箱：沙箱只跑工具片段，Agent推理、Skill加载、会话管理都留在外部常驻进程；多用户场景需要在同一个常驻进程切换Skill，隔离实现难度高。
Agent放进沙箱内部：完整Agent推理循环、Skill加载、会话读写全部运行在独立容器沙箱；每次请求都是独立Agent实例，天然多租户隔离，支持用户私有Skill持久化存储到NAS。

### 延伸补充（客户追问备选）
Q：Agent在外调用沙箱的架构，能不能改造实现多用户私有Skill？
A：技术上可行，但改造成本极高。需要在外部常驻Agent实现Skill热隔离、独立进程池，每个用户单独分配Agent子进程，内存开销大幅上涨；同时很难复用平台NAS挂载、容器自动消杀能力，后续维护成本高。而Agent内化沙箱方案可以直接复用平台原生容器能力，架构简洁，改动量更小。

## 三、PPT精简金句（可直接复制到幻灯片）
1. 双层沙箱防护：外层AIO平台容器沙箱做整套Agent环境硬隔离；内层OpenCode自带沙箱，对工具代码做二次细粒度隔离，双层安全保障。
2. Agent是否内化进沙箱，核心不是代码存放位置，而是Agent的推理循环、Skill加载器、会话读写这套完整运行上下文放在哪里，这就是两种架构本质差别。
