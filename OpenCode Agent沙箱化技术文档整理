# OpenCode Agent 沙箱化全流程落地技术总结文档
## 一、项目背景与初始问题
### 1.1 业务目标
将离线 OpenCode Agent 能力 **全量沙箱化部署**，实现：
1. 多用户环境完全隔离、数据完全隔离
2. 用户自定义 Skill 可动态上传、增量更新、无需发版
3. Agent 会话历史、日志、执行产物永久持久化，**跨沙箱不丢失**
4. 完全适配平台 Sidebox/AIO 临时容器、900s 自动消杀机制
5. 规避用户自定义脚本、自定义 Skill 带来的安全风险

### 1.2 原始裸跑模式存在的核心问题
1. **沙箱销毁即数据全丢**
原生 OpenCode 会话、日志、缓存全部存在容器临时目录，沙箱销毁所有记忆全部丢失，无法实现连续对话。

2. **Skill 无法用户化、无法动态迭代**
原有方案 Skill 必须打进代码/镜像，用户无法新增、修改技能，每次更新必须重新打包镜像、发版，完全不支持用户自定义插件生态。

3. **无用户隔离，多用户数据互相污染**
所有用户共用一套环境、一套会话目录、一套 Skill，无法做租户隔离。

4. **运行权限混乱、频繁报错**
原生运行方式存在 sudo / 普通用户切换，文件属主错乱、权限报错、日志乱输出。

5. **不适配平台沙箱消杀机制**
Agent 如果常驻、数据在容器内，一旦平台 900s 消杀，任务中断、数据全无、无法续跑。

---

## 二、架构选型：两种行业标准方案对比（核心设计）
### 2.1 方案一：Agent常驻外部，沙箱仅做工具执行（业界通用旧方案）
#### 架构逻辑
- Agent 推理、Skill 加载、会话管理全部在**外部常驻服务**
- 沙箱只负责执行「代码片段/工具脚本」
- 沙箱执行完立刻销毁，不持有任何 Agent 上下文

#### 致命缺陷（我方完全不可用）
1. **无法支持用户私有 Skill**
Skill 加载逻辑在常驻服务，多用户并发会出现 Skill 缓存串用户、互相污染，无法实现千人千面。
2. **无完整环境隔离**
只隔离代码执行片段，Agent 主体、配置、上下文仍在业务机，存在安全逃逸风险。
3. **无法适配沙箱消杀**
常驻 Agent 无法适配平台一次性、用完即毁的设计。
4. **无法跨沙箱持久化会话**
会话存在外部服务，无法结合用户级 NAS 做永久保存。

---

### 2.2 方案二：Agent全量内化沙箱（我方最终落地架构）
#### 核心设计思想
**程序镜像固化、动态数据NAS持久化、Skill用户外置、Agent完整运行在沙箱内**

1. **镜像层（只读稳定）**
内置 OpenCode 内核、运行时、基础依赖、基础能力，版本稳定、秒级拉起。

2. **沙箱运行层（一次性、隔离）**
每个用户请求生成**独立全新沙箱**，Agent 完整推理循环、Skill 加载、工具调用全部在沙箱内部完成。

3. **NAS持久化层（用户永久）**
平台为每个用户分配独立 NAS 目录，实现：
- 会话数据库持久化
- 运行日志持久化
- 任务产物持久化
- 用户 Skill 插件持久化、增量更新

---

## 三、最终整体架构图（文字版，可复制到DrawIO/PPT画图）
